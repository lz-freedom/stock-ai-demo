第一部分：
    顶部横幅指标滚动起来，做到循环滚动，头尾相接的那种，切勿做成滚动到结尾后又从头开始的那种,并按顺序展示这些数据
        现价（current_price）
        涨跌额（change_amount）
        涨跌幅（change_percent）
        市场状态（market_state）
        正常盘时间戳（regular_market_time）
        盘前价（pre_market_price）
        盘前涨跌额（pre_market_change）
        盘前涨跌幅（pre_market_change_percent）
        盘前时间戳（pre_market_time）
        盘后价（post_market_price）
        盘后涨跌额（post_market_change）
        盘后涨跌幅（post_market_percent）
        盘后时间戳（post_market_time）
        开盘价（open）
        昨收（previous_close）
        日内最高（day_high）
        日内最低（day_low）
        均价/均线口径均价（average_price）
        振幅（amplitude）
        成交量（volume）
        成交额（turnover）
        换手率（turnover_rate）
        量比（volume_ratio）
        买卖盘比（bid_ask_ratio）
        总市值（market_cap）
        流通市值（float_market_cap）
        总股本（shares_outstanding）
        流通股本（float_shares）
        推算总股本（implied_shares_outstanding）
        52周最高（fifty_two_week_high）
        52周最低（fifty_two_week_low）
        52周区间字符串（fifty_two_week_range）
        历史最高（all_time_high）
        历史最低（all_time_low）
        市盈率TTM（pe_ttm）
        静态市盈率（pe_static）
        预测市盈率（pe_forward）
        市净率PB（pb_ratio）
        EPS(TTM)（eps_ttm）
        EPS(Forward)（eps_forward）
        每股净资产（book_value）
        股息（dividend_rate）
        股息率（dividend_yield）
        最近拆股比例（last_split_factor）
        最近拆股日期（last_split_date）
        Beta（beta）
        50日均线（fifty_day_average）
        200日均线（two_hundred_day_average）
        距50日均线差值（fifty_day_average_change）
        距50日均线百分比（fifty_day_average_change_percent）
        距200日均线差值（two_hundred_day_average_change）
        距200日均线百分比（two_hundred_day_average_change_percent）
        YTD 回报（year_to_date_return）
        YTD 统计区间（year_to_date_trading_date_range）
        3个月回报（three_month_return）
        3个月区间（three_month_trading_date_range）
        6个月回报（six_month_return）
        6个月区间（six_month_trading_date_range）
        1年回报（one_year_return）
        1年区间（one_year_trading_date_range）
        3年回报（three_year_return）
        3年区间（three_year_trading_date_range）
        5年回报（five_year_return）
        5年区间（five_year_trading_date_range）
        52周涨跌幅（fifty_two_week_change_percent）
        标普52周对比（sand_p_52_week_change）
        每股营收（revenue_per_share）
        营收增速（revenue_growth）
        归母净利润（net_income_to_common）
        盈利增速（earnings_growth）
        季度盈利增速（earnings_quarterly_growth）
        EBITDA（ebitda）
        EV/Revenue（enterprise_to_revenue）
        EV/EBITDA（enterprise_to_ebitda）
        ROA（return_on_assets）
        ROE（return_on_equity）
        自由现金流（free_cashflow）
        经营现金流（operating_cashflow）
        总现金（total_cash）
        每股现金（total_cash_per_share）
        总负债（total_debt）
        资产负债率口径（debt_to_equity）
        利润率（profit_margin）
        毛利率（gross_margin）
        EBITDA Margin（ebitda_margin）
        营业利润率（operating_margin）
        trailing EPS（trailing_eps）
        内部人持股（held_percent_insiders）
        机构持股（held_percent_institutions）
        空头股数（shares_short）
        上月空头股数（shares_short_prior_month）
        上月空头统计日期（shares_short_previous_month_date）
        空头统计日期（date_short_interest）
        空头占总股本（shares_percent_shares_out）
        空头回补天数（short_ratio）
        空头占流通股（short_percent_of_float）

第二部分：
    去掉 Strong Buy， 使用market_state替代，market_state的值有：PRE,REG,POST,POSTPOST，请构建对应映射关系合理展示
    展示格式如下：
              股票名称  药丸格式市场状态（不同颜色）                        现价
        Logo  股票代码｜交易所logo+交易所code                               涨跌额+（涨跌幅）
              国家logo + 国家code ｜币种code                                数据更新时间exchange_timezone_short_name + regular_market_time

第三部分：
    展示字段 
        市值：market_cap
        P/E (TTM)：pe_ttm
        P/B：pb_ratio
        Dividend (Yield)：dividend_yield
        Volume：volume
        52wk Range：fifty_two_week_range
        Beta：beta

第四部分：
    展示字段 
    年初以来回报率：year_to_date_return
    近3个月回报率：three_month_return
    近6个月回报率：six_month_return
    近1年回报率：one_year_return
    近3年回报率：three_year_return
    近5年回报率：five_year_return